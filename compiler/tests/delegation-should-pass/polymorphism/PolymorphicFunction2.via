/* polymorphic function with constraints */
fun f{X, Y, Z}(a: int{X}, b: int{Y}, c:out int{Z}) where ((X | Y)<- & (X & Y)-> <: Z) {
    out z = x + y;
}

fun main() {
    val a: int{alice} = input int from alice;
    val b: int{bob} = input int from bob;

    f(a, b, val z);
}

host alice
host bob
