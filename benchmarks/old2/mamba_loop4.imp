process main {
    int{A & B<-} a[100];
    int{A ⊓ B} c = 0;

    for (int{A & B<-} j = 0; j < 100; j+=1) {
        a[j] = j;
    }

    for (int i = 0; i < 100; i+=1) {
        c = c + declassify(a[i], {(A|B)-> & (A&B)<-});
    }
}

host alice : {A};
host bob   : {B};
