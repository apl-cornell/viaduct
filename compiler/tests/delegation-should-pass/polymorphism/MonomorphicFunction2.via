/* verbose monomorphic syntax */
fun f{}(x: int{alice}, y: int{bob}, z:out int{(alice | bob)<- & (alice & bob)->}) where () {
    out z = x + y;
}

fun main() {
    val a: int{alice} = input int from alice;
    val b: int{bob} = input int from bob;

    f(a, b, val z);
}

host alice
host bob
