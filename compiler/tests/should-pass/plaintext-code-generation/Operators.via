host god : {⊥}

process main {
    /* Comparison Operators */
     val a : bool = 1 == 1;
     val b : bool = 1 != 2;

    val c : bool = 1 < 2;
    val d : bool = 1 <= 1;
    val e : bool = 1 <= 2;

    val f : bool = 2 > 1;
    val g : bool = 1 >= 1;
    val h : bool = 2 >= 1;

    /* Arithmetic Operators */
    val i : bool = -1 < 1;
    val j : bool = (-1 + 1) == 0;

    val k : bool = (1 + 2) == 3;
    val l : bool = (3 - 1) == 2;
    val m : bool = (1 * 2) == 2;
    val n : bool = (2 / 1) == 2;

    val o : bool = min(1, 2) == 1;
    val p : bool = max(1, 2) == 2;

    /** Logical Operators */
    val q : bool = true;
    val r : bool = !false;
    val s : bool = true && true;
    val t : bool = false || true;

    output a to god;
    output b to god;
    output c to god;
    output d to god;
    output e to god;
    output f to god;
    output g to god;
    output h to god;
    output i to god;
    output j to god;
    output k to god;
    output l to god;
    output m to god;
    output n to god;
    output o to god;
    output p to god;
    output q to god;
    output r to god;
    output s to god;
    output t to god;
}
