fun {X, Z}f(a: int{X}, b: int{bob}, c: out int{Z}) where (X <: alice, alice ⊓ bob <: Z) {
  out c = a + b;
}

fun main() {
  val x : int = input int from alice;
  val y : int = input int from bob;
  f(x, y, val z);
}

host alice
host bob
