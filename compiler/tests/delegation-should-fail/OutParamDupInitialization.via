/* OutParameterInitializationError */

fun add10{(Alice & Bob)<-}(a: int{Alice}, b: out int{Bob}) {
    out b = 10;
    out b = 20;
}

process main {
    val array = Array[int]{Alice}(10);
    val a: int{Alice} = 0;
    add10(a, val b);
    output a to Alice;
}

hosts: Alice, Bob

