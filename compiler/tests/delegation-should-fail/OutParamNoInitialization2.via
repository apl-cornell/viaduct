/* OutParameterInitializationError */

fun add10(a: int{Alice}, b: out int{Bob}) {
    val x: int = b + 10;
}

process main {
    val array = Array[int]{Alice}(10);
    val a: int{Alice} = 0;
    add10(a, val b);
    output a to Alice;
}

hosts: Alice, Bob

