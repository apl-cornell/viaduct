/* Millionaire's problem with a trusted third party. */
process main {
  int{A & B<-} a;
  int{B & A<-} b;
  bool{A & B} b_richer;

  a = 5;
  b = 10;

  b_richer = a <= b;

  assert b_richer;
}

host trusted : {A & B};
