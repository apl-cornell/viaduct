process main {
    int{A} a[100];
    int{B} b[100];

    int a_sum;
    a_sum = 0;
    int ai;
    for (ai = 0; ai < 100; ai += 1) {
        a_sum += a[ai];
    }

    int b_sum;
    b_sum = 0;
    int bi;
    for (bi = 0; bi < 100; bi += 1) {
        b_sum += b[bi];
    }

    int scale;
    scale = endorse(a_sum, {A & B<-}) + endorse(b_sum, {B & A<-});
    int sum;
    sum = 0;
    int j;
    for (j = 0; j < 100; j += 1) {
        sum += scale * j;
    }

    int{A ⊓ B} prodsum;
    prodsum = declassify(sum, {A ⊓ B});
}

host alice : {A};
host bob   : {B};
