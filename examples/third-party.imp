/* Millionaire's problem with a trusted third party. */
process main {
  a : int{A & B<-};
  b : int{B & A<-};
  b_richer : bool{A & B};

  a = 5;
  b = 10;

  b_richer = a <= b;

  assert b_richer;
}

host trusted : {A & B};
