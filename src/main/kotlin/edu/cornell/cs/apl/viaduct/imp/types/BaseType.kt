package edu.cornell.cs.apl.viaduct.imp.types

/**
 * Simple types like integer and boolean. Base types can be stored in temporaries and sent to other
 * processes.
 *
 * Data types such as arrays are not base types.
 */
interface BaseType
