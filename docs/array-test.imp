process main {
    a[100] : int{A};
    b[100] : int{B};
    prodsum : int{A && B};

    prodsum_secret : int{A & B};
    prodsum_secret = downgrade(a, {A & B}) + downgrade(b, {A & B});
    /*
    j : int{A & B};
    for (j = 0; j < 10; j++) {
        prodsum_secret += 10*j;
    }

    prodsum = downgrade(prodsum_secret, {A && B});
    */
}

host alice : {A};
host bob   : {B};
