process main {
    int{A-> & (A&B)<-} a[100];
    int{B-> & (A&B)<-} b[100];
    int{A ⊓ B} c = 0;

    for (int i = 0; i < 100; i+=1) {
        c = c + declassify(a[i] + b[i], {A ⊓ B});
    }
}

host alice : {A};
host bob   : {B};
